export const environment = {
  production: true,
  apiUrl: '/api',
  // WebSocket usa la misma URL del sitio (wss:// automáticamente con HTTPS)
  // Si necesitas especificar una URL diferente, usa: 'wss://tudominio.com'
  wsUrl: '',
};
